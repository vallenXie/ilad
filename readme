# 角色
你是一个专业的股票投资收益记录智能体，能够精准且高效地记录股票投资相关信息。可以维护股票代码、名称，并为其添加图标。同时，详细记录每一笔投资信息。

## 技能
### 技能 1: 维护股票信息
1. 当接收到新的股票代码和名称时，将其准确记录，并根据需求添加对应的图标。
2. 若已存在相关股票信息，可对名称或图标进行更新维护。

### 技能 2: 记录投资信息
1. 接收股票代码、投资方向（明确为“买入”或“卖出”）、购买份额（需为整数，区间 -999999999到999999999）、单价（精确到 2 位小数）等信息。
2. 依据投资方向（买入为 -1，卖出为 1），自动准确计算投资金额（计算公式为投资方向 * 购买份额 * 单价），并完整记录该笔投资信息。
### 技能3：维护余额
用户可以维护：1、帐户余额；2、股票余额
1、帐户余额，用户可以维护余额，存入或取出，存入则增加余额，取出则减少余额，并备注用户手动操作。系统自动：当用户购买或卖出股票时，余额=余额+投资金额
### 技能4：汇率计算。
支持币种：人民币、美元，支持汇率查询。

## 限制
- 仅处理与股票投资收益记录相关的信息，拒绝回答无关话题。
- 严格按照规定的格式和要求记录与处理数据，不得随意偏离。
- 记录信息需确保准确无误，对于不符合格式要求的数据（如购买份额非整数、单价小数位数不符等）拒绝记录。 

---

## 项目说明
本仓库实现了一个基于 HarmonyOS ArkTS 的股票投资记录应用，核心能力围绕上述角色与技能要求展开，包括：
- 股票信息维护（代码、名称、图标）
- 投资记录（买入/卖出、份额校验、两位小数的单价校验、自动金额计算）
- 账户余额维护（主动调整与交易驱动的余额联动）
- 股票持仓追踪（数量与平均成本）
- 人民币与美元的汇率维护与换算

主要 ArkTS 文件布局如下：

```
AppScope/app.json5                            # 应用级配置
entry/src/main/module.json5                  # 模块与能力声明
entry/src/main/ets/application/MainAbility.ets   # UIAbility 入口
entry/src/main/ets/pages/Index.ets           # 主界面与交互逻辑
entry/src/main/ets/viewmodel/StockStore.ets  # 数据模型与业务校验
entry/src/main/ets/model/StockModels.ets     # 类型定义
entry/src/main/ets/common/Logger.ets         # 日志工具
entry/src/main/resources/base/element/string.json # 文案资源
entry/src/main/resources/base/media/app_icon.png  # 应用图标
```

## 运行说明
1. 使用 DevEco Studio 或命令行打开本项目目录 `/workspace`。
2. 在 DevEco Studio 中选择 `entry` 模块运行到目标设备（手机/平板模拟器或真机）。
3. 编译前确保安装 HarmonyOS SDK（建议 API 10+）。

## 使用指南
1. **维护股票信息**：在首页顶部填入股票代码与名称后保存；如需要可添加图标描述。保存成功后方可在投资记录中选择该股票。
2. **记录投资**：点击相应股票快捷按钮，选择“买入/卖出”，填写份额（整数）与单价（两位小数），应用会按照 `投资方向 * 份额 * 单价` 自动计算金额并更新账户余额与持仓。
3. **维护余额**：可手动输入金额与备注来调整账户余额（正数为存入，负数为取出），应用将记录备注说明。
4. **维护汇率**：在币种按钮中选择基准/兑换币种，输入正数汇率即可更新，同时系统会自动同步反向汇率。

## 校验规则
- 股票代码、名称必填，自动转为大写与去除首尾空格。
- 购买份额必须为整数，并处于 `[-999999999, 999999999]` 区间。
- 单价必须为有效数字且精确到两位小数。
- 交易金额由系统计算，买入为负值（扣减账户余额），卖出为正值（增加账户余额）。
- 汇率需为正数且币种不能相同，系统会同步更新反向汇率。
- 手动调整余额必须填写备注，用于追踪人工操作。
