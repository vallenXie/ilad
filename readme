# 鸿蒙 Stage 应用示例

## 项目简介
- 基于 ArkTS 的鸿蒙 Stage 模型示例应用。
- 展示一个简单的计数器页面，演示基本状态管理和组件布局。

## 功能特性
- 采用 `UIAbility` + ArkTS 声明式 UI 结构。
- 通过 `@State` 管理页面状态，支持计数器增减与重置。
- 提供基础的资源配置（字符串、颜色、图标）。

## 目录结构
```
AppScope/app.json5                  # 应用级别配置
entry/src/main/module.json5         # 模块配置（Stage 模型）
entry/src/main/ets/
  ├─ Application/MyAbilityStage.ts  # AbilityStage 入口
  ├─ entryability/EntryAbility.ts   # UIAbility 生命周期逻辑
  └─ pages/Index.ets                # ArkTS 主页面
entry/src/main/resources/           # 字符串、颜色、页面等资源
build-profile.json5                 # 构建配置（需自行配置签名文件）
oh-package.json5                    # 项目依赖配置
hvigorfile.ts                       # hvigor 构建脚本
```

## 环境要求
- DevEco Studio 5.0+ 或命令行构建工具链（包含 hvigor、ohpm）。
- HarmonyOS SDK 10.0 (API 10) 及以上版本。

## 快速开始
1. 安装依赖：
   ```bash
   ohpm install
   ```
2. 使用 hvigor 构建调试包：
   ```bash
   hvigor assembleDebug
   ```
   > 首次构建前请在 `build-profile.json5` 中填入实际的调试签名 `profile` 与 `certificate` 文件路径。
3. 在 DevEco Studio 中导入本项目，连接设备或启动模拟器，运行 `entry` 模块即可查看效果。

## 关键代码说明
- `EntryAbility.onWindowStageCreate` 负责加载 `pages/Index` 页面。
- `Index.ets` 使用 ArkTS 声明式语法构建计数器页面，演示 `Column`、`Row`、`Text`、`Button` 等基础组件。

## 后续可扩展方向
- 接入网络请求与数据存储，构建实际业务场景。
- 新增多 Ability、多页面或分包模块。
- 为不同设备形态（如穿戴、车机）提供适配布局。

